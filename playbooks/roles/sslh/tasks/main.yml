---
- name: Install sslh and dependencies from apt
  apt: name={{ item }}
  with_items:
    - sslh

- name: Copy sslh port multiplexer configuration file into place
  template: src=sslh.j2
        dest=/etc/default/sslh
  notify: Restart sslh
